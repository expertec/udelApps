# Google Gemini API (para análisis de video)
# Obtén tu API key en: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=tu_api_key_aqui

# Modelos de Gemini (opcional, usa valores por defecto si no se especifican)
# Opciones recomendadas: gemini-2.0-flash-exp, gemini-1.5-pro, gemini-1.5-flash
# Si no se especifican, usa gemini-2.0-flash-exp por defecto
GEMINI_VIDEO_MODEL=gemini-2.0-flash-exp
GEMINI_TEXT_MODEL=gemini-2.0-flash-exp

# OpenAI API (para generación de cartas descriptivas - más confiable)
# Obtén tu API key en: https://platform.openai.com/api-keys
# Si se configura, se usará OpenAI en lugar de Gemini para cartas descriptivas
OPENAI_API_KEY=tu_openai_api_key_aqui

# Firebase Service Account
# Descarga el JSON desde Firebase Console → Project Settings → Service Accounts
# Copia TODO el contenido del JSON como una sola línea
FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"..."}

# Vimeo API (opcional, para subir videos aprobados)
# Obtén tu token en: https://developer.vimeo.com/apps
# Necesitas permisos: upload, edit, video_files
VIMEO_ACCESS_TOKEN=tu_vimeo_token_aqui

# Puerto del servidor (opcional, Render lo asigna automáticamente)
PORT=10000

# Ambiente (opcional)
NODE_ENV=production
